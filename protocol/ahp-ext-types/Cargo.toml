[package]
name = "himark-ahp-ext-types"
version = "0.1.0"
edition = "2021"

[dependencies]
himark-text = { path = "../../frontend/text", package = "text" }
operation = { path = "../../frontend/operation" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
