[package]
name = "hifind"
version = "0.1.0"
edition = "2021"

[dependencies]
grep-regex = "0.1"
grep-searcher = "0.1"
himark-ahp-ext-types = { path = "../../protocol/ahp-ext-types" }
ignore = "0.4"
regex = "1"

[dev-dependencies]
tempfile = "3"
