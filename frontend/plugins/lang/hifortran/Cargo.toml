[package]
name = "hifortran"
version = "0.1.0"
edition = "2021"

[dependencies]
editor = { path = "../../../editor" }
hisitter = { path = "../../../hisitter" }

[target.'cfg(not(target_os = "emscripten"))'.dependencies]
tree-sitter-fortran = "0.6.0"

[dev-dependencies]
imba = { path = "../../../imba" }
