[package]
name = "hidart"
version = "0.1.0"
edition = "2021"

[dependencies]
editor = { path = "../../../editor" }
hisitter = { path = "../../../hisitter" }

[target.'cfg(not(target_os = "emscripten"))'.dependencies]
tree-sitter-dart = "0.2.0"

[dev-dependencies]
imba = { path = "../../../imba" }
