[package]
name = "hizig"
version = "0.1.0"
edition = "2021"

[dependencies]
editor = { path = "../../../editor" }
hisitter = { path = "../../../hisitter" }

[target.'cfg(not(target_os = "emscripten"))'.dependencies]
tree-sitter-zig = "1.1.2"

[dev-dependencies]
imba = { path = "../../../imba" }
