[package]
name = "higallery"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
himark = { path = "../../himark" }
imba = { path = "../../imba" }
skia-safe = { version = "0.99.0", features = ["textlayout"] }

[dev-dependencies]
himark = { path = "../../himark", features = ["test-support"] }
